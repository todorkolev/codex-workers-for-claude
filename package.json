{
  "name": "codex-workers-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local MCP bridge that lets Claude Code drive Codex as steerable workers.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0"
  }
}
